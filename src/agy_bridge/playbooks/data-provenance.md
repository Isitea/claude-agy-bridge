# 데이터 출처 (data-provenance)

확인하라:

- 하드코딩된 상수마다 출처를 물어라: 계산값인가, 문헌값인가, 추정값인가.
  구분 표기가 없으면 지적하라.
- 물리 상수의 값·유효숫자·단위가 표준값(CODATA 등)과 일치하는가.
- 서로 다른 출처의 데이터를 섞을 때 기준 상태·온도·방법론이 호환되는가.
- 임의로 보이는 계수(fudge factor, 보정 계수)를 찾아 근거를 요구하라.
- 데이터의 적용 범위(측정 조건)를 벗어난 사용이 있는가.
